{
  "title": "PublisherID",
  "description": "Holds one publisher identifier, e.g. 'manuscript number'",
  "type": "object",
  "properties": {
    "attribute": {
      "type": "string"
    },
    "Identifier": {
      "type": "string"
    }
  }
}
